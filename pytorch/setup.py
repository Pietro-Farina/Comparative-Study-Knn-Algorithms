from setuptools import setup, find_packages

setup(name='protoNN',
      version='0.1',
      description='PyTorch code for ProtoNN algorithms and relative tests',
      packages=find_packages(),
      license='MIT License',
)